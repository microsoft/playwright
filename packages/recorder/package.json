{
  "name": "recorder",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && tsc && node ./utils/generate_injected.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "events": "^3.3.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.266"
  }
}
